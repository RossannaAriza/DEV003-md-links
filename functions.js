//------- Funciones para las options -------
//Funcion total de links
const totalLinks = ( url) => {
//contar todos los links, la longuitud del arreglo
return url.length;
}

//funcion links unicos
const uniqueLinks = (url) => {
// traer en una constante solo los links sin repetir (href) y dar valor
}


//funcion links rotos
const brokenLinks = (url) => {
//validar los  links que en ok retornen FAIL
}

module.exports = {
    totalLinks,
    uniqueLinks,
    brokenLinks
};